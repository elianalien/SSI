#:kivy 1.8.0
#:import KivyLexer kivy.extras.highlight.KivyLexer
#:import Factory kivy.factory.Factory

<ShowcaseScreen>: #this is an app kv layout!!!!!!!!!
#    ScrollView:
#        do_scroll_x: False
#        do_scroll_y: False if root.fullscreen else (content.height > root.height - dp(16))
#        AnchorLayout:
#            size_hint_y: None
#            height: root.height if root.fullscreen else max(root.height, content.height)
#            GridLayout:
#                id: content
#                cols: 1
#                spacing: '8dp'
#                padding: '8dp'
#                size_hint: (1, 1) if root.fullscreen else (.8, None)
#                height: self.height if root.fullscreen else self.minimum_height


BoxLayout:
    orientation: 'vertical'

    #box container
    #canvas.before:
    #    Color:
    #        rgba: 4, 89, 130, 1
    #    Rectangle:
    #        size: self.size
    #        source: 'data/background.png'

    #ActionBar:

    #    ActionView:
    #        id: av
    #        ActionPrevious:
                #with_previous: (False if sm.current_screen.name == 'button' else True) if sm.current_screen else False
    #            title: 'SADANG SERANG INTERAKTIF' 
                #on_release: app.go_higherarchy_previous()


    ScreenManager:
        id: sm
        #sm.transition = 'WipeTransition'
        on_current_screen:
            idx = app.screen_names.index(args[1].name)
            #if idx > -1: app.higherarchy.append(idx)
